# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 따르는 규약이다. 전체 사양은 팀 공유
문서(`single_source_of_truth.md`, 이하 **SSOT**)에 있고 이 파일은 그중 **코드를
고칠 때 실제로 어기기 쉬운 것**만 모았다. SSOT와 충돌하면 SSOT가 우선한다.

## 이 저장소가 하는 일

마이데이터(소득·지출·자산·부채)를 받아 시나리오별 목표 주택가격 대비 필요
자기자본·월 저축액을 계산하고, 예·적금과 대출 상품을 점수화해 포트폴리오와 구매
전략을 비교하는 **결정론적 계산 엔진**이다.

생성형 AI는 계산 결과를 **설명만** 한다. 수치를 만들지도 바꾸지도 않는다.

## 명령

```bash
python -m pip install -e ".[dev]"
python -m pytest -q
python -m ruff check app tests
```

- Python 3.12. 포맷터는 쓰지 않는다 — **`ruff format`을 디렉터리 전체에 돌리지 말 것.**
  린트는 손댄 파일에만 건다.
- `.env`는 저장소에 없다(gitignore). 없어도 전체 테스트가 통과한다.
  DB·AI 키가 필요한 경로는 없으면 결측으로 남고 계산은 그대로 돈다.

## 가장 자주 나오는 결함 한 가지

**모르는 것을 느슨한 쪽으로 뭉개는 것.** 이 저장소에서 발견된 계산 버그는
거의 전부 이 형태였다. 값을 확정하지 못했을 때 "일단 0으로", "일단 기본값으로",
"일단 비규제로" 채우면 한도가 커지거나 계획이 쉬워 보인다.

방향은 도메인마다 다르다. **어느 쪽이 위험한지 먼저 정하고 그 반대로 채운다.**

| 축 | 모를 때 위험한 방향 | 그래서 |
| --- | --- | --- |
| 대출 한도 | 크게 잡는 것 | 구간이면 최솟값, 비율이면 UNKNOWN |
| 예·적금 수익 | 크게 잡는 것 | 우대 미달성·말일 납입·일반과세 |
| 구매 총비용 | 작게 잡는 것 | 부대비용을 못 채우면 시나리오를 만들지 않음 |
| 중도상환수수료 | 작게 잡는 것 | 면제 여부를 모르면 부과되는 쪽 |

## UNKNOWN 계약 (SSOT §22.1)

- 확정할 수 없으면 **추측하지 말고 UNKNOWN**을 반환하고, 어떤 필드가 없는지
  **이름으로** 남긴다. 사용자에게 "계산 불가"가 아니라 "무엇을 알려주면
  계산되는지"로 읽혀야 한다(§19).
- **0과 모름을 같은 값으로 뭉개지 않는다.** 한도 0원과 한도 미상은 다른 상태다.
- **모름과 "해당 없음"도 뭉개지 않는다.** 반대 방향도 마찬가지다 — 필요 대출금액이
  0으로 **산출된** 경우의 월 상환액은 "확인되지 않음"이 아니라 "없음"이다.
- 후보 0건과 "조건을 만족하는 상품이 없음"은 다른 상태다. 조회 실패를 빈 목록으로
  바꾸지 않는다.
- 근거를 확정하지 못한 제도 상수는 구현하지 말고 **명시적으로 막는다.**
  표에 실을 때는 `verified=False`로 두고 기본 조회에서 제외한다.

**예외 — 금액 한도(§22.3).** 조건을 몰라 한도가 구간으로만 확정되면 그 구간의
**최솟값**을 쓰고 가정을 기록한다. 과소평가는 안전하고 과대평가는 아니다.
반대로 **비율 한도**(보증금의 80% 등)는 빠뜨리면 과대평가가 되므로 UNKNOWN으로
되돌린다. **"상한 없음(UNCAPPED)"과 "모름(UNKNOWN)"도 다르다.**

## 단위 — 틀리면 결과가 100배 어긋난다

```python
i_save = (1 + r_annual) ** (1/12) - 1   # 예·적금: 복리 실효환산
i_loan = r_annual / 12                   # 대출: 명목환산 (PMT용)
```

- 금액은 원(KRW) 정수. 표시용 반올림과 계산용 값을 분리한다.
- 기간은 개월. 연 단위 입력은 즉시 환산한다.
- 원천 데이터의 `lend_rate_avg: 5.2`는 **퍼센트 숫자**다. 엔진은 `Decimal("0.052")`를
  받는다. 이 변환은 반드시 `data_pipeline`에서 한다.
- 여유자금은 한 정의만 쓴다: `buffer = max(300_000, essential_expense * Decimal("0.10"))`

## 계층 경계

```
app/db/repositories/     DB 행 → 원천 스키마 (I/O만)
app/data_pipeline/       원천 → 정규화, 단위 변환, 검수표(curated/)
app/rule_engine/         상품별 자격조건 Rule Pack (PASS/FAIL/UNKNOWN)
app/regulations/         법정 상한표. 기준일·출처·시행 종료일 필수
app/engines/             순수 계산. 다른 계층을 import 하지 않는다
app/services/            엔진 조립. 규제 해석 → 판정 → 계산 → 조립
app/reports/             양식·AI 설명·기계 검증
app/api/routes/          HTTP 경계. 시간·난수·ID를 여기서만 만든다
```

- 계산 엔진은 원천 스키마를 직접 소비하지 않는다. 정규화된 값만 받는다.
- 순수 엔진이 `data_pipeline`을 import 해야 할 것 같으면 **Protocol로 주입**한다.
- `as_of`(계산 기준일)를 명시 인자로 넘긴다. 계산 로직에서 `datetime.now()` 금지.

## 보고서

- 수치는 엔진이 렌더링하고 AI는 **서술 칸만** 채운다. 서술 칸에 수치·날짜를 쓰면
  기계 검증이 막는다. 이 규칙이 "값은 맞고 기준만 틀린" 귀속 오류를 구조적으로 없앴다.
- 두 에이전트(작성 → 기계 검증 → 판정)를 모두 통과한 절만 서술이 실린다.
  통과하지 못한 절은 **수치만** 실린다 — 보고서 전체를 버리지 않는다.
- 판정을 받지 못한 상태는 통과가 아니다.
- 보고서 종류를 추가할 때 파이프라인을 복사하지 말고 `NarrationSpec`을 만든다.

## 하지 말 것

- 정책 금리·세율·한도를 하드코딩. 반드시 기준일·출처와 함께 표에 둔다.
- 규제 비율을 **시행 종료일 없이** 갱신. 낡은 값이 새 대책 이후에도 계속 조회된다.
- 지역을 확인 못 했을 때 **비규제로 처리**(LTV 70% vs 40% — 과대평가).
- 스트레스 가산금리를 확정 못 했을 때 **0으로 대체**. 이것만 보수적 하한이 없어
  모르면 계산을 포기한다.
- **DSR을 실제 금리로 판정.** 심사는 '실제 금리 + 스트레스 가산금리'로 한다.
  반대로 월 현금흐름 판정에 스트레스 금리를 쓰면 실제보다 빠듯해진다 — 두 금리는 분리한다.
- DTI 분자에 DSR용 원리금 넣기. DTI는 기타 대출을 **이자만** 센다.
- DTI 한도를 옵션 공통으로 한 번만 계산. 금리·만기가 옵션마다 다르다.
- `loan_lmt` 같은 **자유텍스트 한도를 정규식으로 파싱**하거나 파싱 실패를 임의
  숫자로 대체. 한도는 `data_pipeline/curated/loan_limits.py`를 거친다.
- `join_deny`/`join_member` 같은 **자유텍스트 자격조건을 자동 판정해 탈락 처리.**
  §19 "사용자가 직접 확인할 조건"으로 노출한다.
- 한도표에 **Rule Pack에 없는 새 facts 키**를 만들기. 자격판정과 한도가 다른
  어휘를 쓰면 같은 차주에게 앞뒤가 안 맞는 결과가 나온다. 팩과 표를 함께 고친다.
- 여러 상품의 한도를 **단순히 더하기**. DSR·구매 후 현금흐름은 차주 1인당,
  LTV는 담보 1건당 하나의 예산이라 합치면 이중으로 센다.
- 시나리오에 **임의 확률 부여**(§8.3). 커버리지("몇 개 충족")로만 표현한다.
- 대출 승인·실제 적용금리를 **보장하는 문구** 출력(§20).
- 브라우저가 보낸 계산 결과를 검증 없이 AI 원본으로 신뢰. 서버가 직접 계산한
  값으로 핸드오프를 만든다.

## 데이터와 비밀

- `.env`에 DB 접속정보와 API 키가 들어간다. **읽지 말고 값을 출력하지 말 것.**
  오류 메시지에도 DSN·사용자명을 넣지 않는다.
- `selected_23_products.json`은 추적하지 않는 원천 데이터다. **커밋하지 말 것.**
  ```bash
  git add -A ':!selected_23_products.json'
  ```
- 테스트 픽스처에 운영 DB의 사용자명·주소·포트·비밀번호를 넣지 않는다.
- `sample_data/`에는 가상 데이터만 둔다. 매물 스냅샷은 `source_type: MOCK`이다.

## 협업

- 각자 포크에서 기능 브랜치를 만들어 `2026-KAIB/housing-finance-core`의 `main`으로
  PR을 낸다. `main`에 직접 커밋하지 않는다.
- PR 본문에는 **무엇을 왜 그렇게 정했는지**를 적는다. 특히 어느 방향이 위험해서
  그 기본값을 골랐는지. 리뷰가 그 판단을 검토할 수 있어야 한다.
- 규제·약관 값은 추측하지 말고 **1차 출처를 확인**한다. 확인하지 못하면 확인하지
  못했다고 표시하고 넘어간다.
- 사용자 응답은 한국어로 쓴다.
